{
  "name": "talenomix",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Talenomix — mandate intelligence for Nordic executive search. Static marketing site and synthetic demo.",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "eslint .",
    "verify:claims": "node scripts/verify-claims.mjs",
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium",
    "verify": "npm run check && npm run lint && npm run verify:claims && npm run build && npm run test"
  },
  "dependencies": {
    "@fontsource-variable/manrope": "^5.2.5",
    "@fontsource-variable/newsreader": "^5.2.5",
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@axe-core/playwright": "^4.10.1",
    "@eslint/js": "^9.24.0",
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.14.0",
    "eslint": "^9.24.0",
    "eslint-plugin-astro": "^1.3.1",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.29.1"
  }
}
