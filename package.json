{
  "name": "talenomix",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Talenomix — mandate intelligence for Nordic executive search. Static marketing site and synthetic demo.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "eslint .",
    "verify:claims": "node scripts/verify-claims.mjs",
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium",
    "verify": "npm run check && npm run lint && npm run build && npm run verify:claims && npm run test"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/manrope": "^5.2.5",
    "@fontsource-variable/newsreader": "^5.2.5",
    "@fontsource/instrument-serif": "^5.3.0",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@axe-core/playwright": "^4.10.1",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.52.0",
    "@types/node": "^24.13.3",
    "eslint": "^10.8.0",
    "eslint-plugin-astro": "^3.0.1",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.65.0"
  }
}
